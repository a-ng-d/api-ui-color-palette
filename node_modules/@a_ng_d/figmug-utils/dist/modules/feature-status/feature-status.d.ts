import { Feature, PlanStatus } from '../../types/feature.types';

export default class FeatureStatus<T> {
    features: Array<Feature<T>>;
    featureName: string;
    planStatus: PlanStatus;
    suggestion?: string;
    result?: Feature<T>;
    limit?: number;
    constructor(data: {
        features: Array<Feature<T>>;
        featureName: string;
        planStatus: PlanStatus;
        suggestion?: string;
    });
    getFeature(): Feature<T> | undefined;
    isActive(): boolean;
    isPro(): boolean;
    isNew(): boolean;
    isBlocked(): boolean;
    isReached(current: number): boolean;
    isAvailableAndBlocked(): string | null | undefined;
}
//# sourceMappingURL=feature-status.d.ts.map