import { PaletteData, PaletteDataThemeItem } from '../../types/data.types';
import { MetaConfiguration, BaseConfiguration, ThemeConfiguration, FullConfiguration } from '../../types/configuration.types';

export default class Data {
    private base;
    private themes;
    private meta?;
    private paletteData;
    private currentScale;
    constructor({ base, themes, meta, }: {
        base: BaseConfiguration;
        themes: Array<ThemeConfiguration>;
        meta?: MetaConfiguration;
    });
    searchForShadeStyleId: (themes: Array<PaletteDataThemeItem>, themeId: string, colorId: string, shadeName: string) => string;
    makePaletteData: (previousData?: PaletteData) => PaletteData;
    makePaletteFullData: () => FullConfiguration;
}
//# sourceMappingURL=data.d.ts.map